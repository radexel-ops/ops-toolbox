# -*- coding: utf-8 -*-
"""
OpenAI 프롬프트 정의
"""

# ── 다중 이체건 파싱 프롬프트 ────────────────────────────
MULTI_TRANSFER_PROMPT = r"""
너는 라덱셀 회계·재무 담당자를 위한 AI야. `본문`을 읽고, 한 개 이상의 이체건 정보를 JSON 형태로 반환해.
- 문서 하나에 이체건이 여러 건 섞여 있으면, 각 건마다 별도 객체를 리스트로 반환
- 단일 이체건이면 리스트에 객체 하나만 담아 반환

반환할 JSON 리스트 형태:
[
  {
    "이체_목표": str,   # 예: "제작완료/검수 후", "세금계산서 확인 후", "즉시 이체"
    "구분": str,       # 지정 목록 중 하나. 없으면 "검토 필요"
    "금액": int,       # 부가세 포함, 쉼표·단위 없이 숫자만
    "수취인": str,     # 거래처명. 식별 불가 시 빈 문자열 ""
    "은행": str,       # 예 : "IBK기업은행"
    "계좌번호": int,    # 예 : "13703900504018",
    "내부메모": str,   # "(구분)제품·간략 설명". 제품 없으면 "(구분)내역 확인",
    "제품": str        # saber/blade/space/pilot 중 하나, 없으면 "",
    "비고": "연구비계좌이체(데이터 활용 의료건강)"
  },
  ...
]

세부 지침:
1. **이체건 분리**
   - **지급조건 분리**
     본문에 "지급 조건" 항목이 구분되어 나열되어 있으면
     1) 각 블록을 별도 이체건으로 분리
     2) 블록 내 조건명(예: 계약금, 중도금, 완료금)을 `"이체_목표"`로 사용
     3) 괄호 안 금액(예: `(2,475,000 원)`)을 `"금액"`으로 파싱
     4) 각 블록이 %로 나뉘어져 있으면 잘 계산해서 각 블록에 적절히 값을 적용
     5) 괄호 바깥 설명을 요약하여 `"내부메모"`에 반영

   - **금액/합계 분리**
     본문에 "금액" 또는 "합계" 항목이 여러 개 있으면 각각 별도 객체로 분리
     "총금액" 등 합산된 한 건만 있으면 단일 이체건으로 처리

   - **독립 적용**
     지급조건 분리와 금액/합계 분리는 서로 무관하게 적용.
     두 가지가 모두 있으면, 지급조건별로 먼저 분리한 뒤 각 블록에서 금액/합계 분리를 추가 적용해 모든 이체건을 생성

2. **구분 필드**
   - 허용 값: 기타수수료, 교육비, 부품구매, 외주용역, R&D비품, 지재권, 개발SW,
     착오송금환입, 복지비, PC부속품, 사무잡비, 업무툴, 여비, 인테리어,
     공용비품, 가구, 임대관리비, 접대비, 채용비용, 수익, 급상여, 사회보험,
     관세, 주민세, 원천세, 부가세, 법인세
   - 본문 키워드 매핑, 없으면 "검토 필요"

3. **금액 필드**
   - 쉼표·단위(원, KRW) 제거 후 정수로 변환
   - 파싱 실패 시 0

4. **수취인 필드**
   - 본문을 전반적으로 파악해서 `구입처`, `거래처`, `예금주` 등에서 추출.
   - 불명확 시 빈 문자열

5. **은행 / 계좌번호**
   - 본문을 전반적으로 파악해서 '입금 계좌', '계좌 정보' 등의 행에서 은행명과 계좌번호 추출
   - 다수 후보면 첫 번째 사용, 없으면 빈 문자열
   - **여비정산 객체는 항상 빈 문자열** (스크립트가 후처리)

6. **제품 필드**
   - 매핑 우선순위: magsaver/세이버/saber→"saber", magblade/블레이드/blade→"blade",
     스페이서/magspacer/space→"space", magpilot/파일럿/pilot→"pilot"
   - 다중 매칭 시 우선순위대로

7. **내부메모 필드**
   - "(구분)제품·간략 설명" 형태.
   - 공백 포함 20자 이내로 작성.
   - 구분이 "지재권"인 경우, 반드시 "(지재권)<청구서번호>" 형태로, 예: "(지재권) SPS25-2028"
   - 제품 정보 없으면 "(구분)내역 확인"
   - **여비정산**이면 `"(여비정산){기안자명}{귀속월}여비정산"` 형식 (예: "(여비정산)김철수05월여비정산")


8. **이체_목표 필드**
   - 본문을 전반적으로 파악해서 언제까지 이체해야하는지 작성하는 것이며, 지급조건 분할 시 블록 조건명 등이 가능함.
   - 그 외 본문 내 이체 조건(예: 제작완료/검수 후, 세금계산서 확인 후, 즉시 이체)
   - 언급 없으면 "세금계산서 확인 후"
   - **여비정산**이면 오늘 기준 **다가오는 가장 가까운 매월 2번째 목요일**을 `YYYYMMDD` 형식으로 작성


9. **비고 필드**
   - 최우선: 본문을 전반적으로 분석해서, 운영계좌로 이체해야하는지, 정부지원금 계좌에서 이체해야하는지 파악 후, 다음 예시와 같이 작성.
     · 예) "운영계좌이체", "연구비계좌이체", "데이터활용과제 지원금 계좌 이체" 등의 방식으로 적절히 작성.
   - 그런 문구가 없으면 본문에 나타나지만 다른 열로 옮길 곳이 없는 핵심 정보를 간결히 기입
   - 쓸 내용이 전혀 없으면 빈 문자열

10. **여비정산(출장 경비) 특별 규칙**
    - **판별**
      제목에 "지출승인요청", "여비정산", "출장경비" 등이 있거나
      본문에 `이동구간`, `출장목적`, `유류비`, `통행료`, `네이버 길찾기 캡처` 등이 반복되면 여비정산으로 간주
      애매하면 여비정산 아님으로 처리
    - **객체 생성**
      여비정산으로 판단되면 **모든 행을 합산해 단일 객체**만 반환
    - **필드 값**
      * 구분: "여비정산"
      * 금액: "합계(총 청구금액)" 숫자, 없으면 행별 금액 합계
      * 수취인: 기안자명 추출
      * 은행/계좌번호: 빈 문자열 (스크립트 후처리)
      * 제품: 빈 문자열
      * 내부메모, 이체_목표, 비고: 위 규칙 7‧8‧9에 따라 작성

출력 규칙:
- 반드시 JSON 리스트만 출력
- 다른 텍스트 절대 금지
- 리스트 안 객체 순서 유지
"""

# ── 세금계산서 매칭 프롬프트 템플릿 ──────────────────────
MATCH_PROMPT_TEMPLATE = """당신은 회계 전문가입니다.

▼ 세금계산서 한 건(invoice) ▼
{invoice_json}

▼ AP 후보 리스트(cands) ▼
{ap_rows_json}

규칙:
1) 같은 결제건이라면 cand 의 row_index 숫자만 답한다.
2) 전부 다른 결제건이면 -1 만 답한다.
3) 숫자 이외 다른 문자는 절대 쓰지 마라.
판단 기준: 승인번호·상호·품목·금액·청구서번호(SPSxx-xxxx) 등 종합. (특허의 경우, 해외비용이 많이 발생하기 때문에 세금계산서 항목과 이체하는 항목의 금액이 차이가 클수 있음. 대신 청구서 번호가 같다면 일치할 확률이 매우 높음.). 매월 반복적으로 지출되는 항목들이 있는데(임대비, 월자문료 등)이런 것들은 기존행과 금액과 이름이 같이 때문에 헷갈릴 수 있는데, 신규행으로 추가해야되며, 특히 ap데이터에 이미 세금계산서 번호가 적혀있는 경우에는 다른 모든 정보가 같더라도 다른 결제건일 확률이 높다."""

# ── 신규 AP 행 생성 프롬프트 ─────────────────────────────
NEW_ROW_PROMPT = """너는 라덱셀 회계팀을 위한 AI야. 아래 'invoice'는 회사가 받은 세금계산서 한 건이야.
이걸 보고 새로운 AP List 행을 만들 때 필요한 4개 값을 JSON 으로만 반환해.

반환 형식(예):
{
  "구분": "외주용역",
  "은행": "확인 필요",
  "계좌번호": "확인 필요",
  "내부메모": "(외주용역)Blade 모터 개발"
}

규칙:
1. 구분 필드는 다음 목록 중 하나: 기타수수료, 교육비, 부품구매, 외주용역, R&D비품, 지재권, 개발SW, 착오송금환입, 복지비, PC부속품, 사무잡비, 업무툴, 여비, 인테리어, 공용비품, 가구, 임대관리비, 접대비, 채용비용, 수익, 급상여, 사회보험, 관세, 주민세, 원천세, 부가세, 법인세. 없으면 '검토 필요'.
2. 내부메모는 '(구분)제품·간략 설명' 형식, 20자 이내. 단, 제품은 'saber', 'blade', 'spacer', 'pilot' 네가지만 공식적으로 존재함. 데이터에는 magsaber, 세이버, 맥세이버 등 비슷한 다른 형태로 적혀있을 수 있으나, 표기는 반드시 공식 4가지 용어로만 사용할 것. 제품 정보 없으면 '(구분)내역 확인'.
3. 정보 부족한 은행·계좌번호는 '확인 필요'.
4. JSON 외 다른 텍스트 절대 금지."""

# ── 두 번째 목요일 계산 프롬프트 템플릿 ──────────────────
SECOND_THURSDAY_PROMPT_TEMPLATE = (
    "오늘({today}) 이후 가장 가까운 월의 두 번째 목요일 날짜만, "
    "YYYYMMDD 형식으로 답하십시오.(예 : 오늘이 2025년 6월 1일 인 경우, 20250601 으로 출력. "
    "오늘이 2025년 10월 1일인 경우, 20251001 으로 출력 )"
)
